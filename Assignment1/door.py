from lock import Lock 
from handle import Handle

class Door:
    def __init__(self, woodType : str, height : float, diameter = Lock.DEFAULT_DIAMETER, state = Lock.DEFAULT_STATE):
         self._woodType : str = woodType
         self.height : float

    @property
    def (self, parameter_list):
        return
